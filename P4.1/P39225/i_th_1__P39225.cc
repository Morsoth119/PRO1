#include <iostream>
using namespace std;

int main() {
  int i, x;
  cin >> i;
  int a = 1;

  while (cin >> x and a < i) {
    ++a;
  }

  cout << "At the position " << i << " there is a(n) " << x  << "." << endl;
}
